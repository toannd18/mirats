import { useEffect, useState } from 'react';import { Table, Button, Space, message } from 'antd';import { ReloadOutlined } from '@ant-design/icons';import apiClient from '../../../services/api-client';export default function DepreciationListPage(){const[data,setData]=useState<any[]>([]);const[loading,setLoading]=useState(false);const fetch=async()=>{setLoading(true);try{const r=await apiClient.get('/depreciations');setData(r.data.data)}catch{message.error('Lỗi')}setLoading(false)};useEffect(()=>{fetch()},[]);const columns=[{title:'Tên',dataIndex:'name'},{title:'Số tháng',dataIndex:'months'}];return(<div><Space style={{marginBottom:16}}><Button icon={<ReloadOutlined/>} onClick={fetch}/></Space><Table columns={columns} dataSource={data} rowKey="id" loading={loading} size="small" scroll={{ x: 'max-content' }}/></div>)}
